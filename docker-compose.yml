version: "3.9"

services:
  db:
    image: postgres:16
    container_name: app_db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app_network

  api:
    build: .
    container_name: app_api
    env_file:
      - .env
    depends_on:
      - db
    ports:
      - "8000:8000"
    volumes:
      - .:/code
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    networks:
      - app_network

  bot:
    build: .
    container_name: app_bot
    env_file:
      - .env
    depends_on:
      - db
    volumes:
      - .:/code
    command: python -m app.bot.handlers
    networks:
      - app_network

volumes:
  postgres_data:

networks:
  app_network:
    driver: bridge
